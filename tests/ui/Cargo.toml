[package]
name = "csa-ui-harness"
version = "0.0.0"
edition = "2024"
rust-version = "1.89"
publish = false

[dependencies]
ratatui = { version = "=0.30.2", default-features = false, features = ["crossterm"] }

[lints.rust]
unsafe_code = "forbid"
